<?xml version="1.0" encoding="utf-8"?>
<!-- versionCode - An integer value that represents the version of the application code, relative to other versions. -->
<!-- versionName - The value is a string so that you can describe the application version as a <major>.<minor>.<point> string, or as any other type of absolute or relative version identifier. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lt.appcamp.lab.mylab1"
    android:versionCode="1"
    android:versionName="1.0" >

    <!-- minSdkVersion - An integer designating the minimum API Level required for the application to run -->
    <!-- targetSdkVersion - An integer designating the API Level that the application targets. -->
    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Light" >
        <activity
            android:name="lt.appcamp.lab.mylab1.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>